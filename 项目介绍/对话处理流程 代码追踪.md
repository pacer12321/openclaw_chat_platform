# 对话处理流程 代码追踪

## 目录
1. [概述](#概述)
2. [请求入口：/api/chat](#请求入口-apichat)
3. [Agent 执行：流式响应处理](#agent-执行流式响应处理)
4. [Guardian 安全审查](#guardian-安全审查)
5. [记忆注入机制](#记忆注入机制)
6. [会话持久化](#会话持久化)
7. [错误恢复流程](#错误恢复流程)
8. [关键代码片段汇总](#关键代码片段汇总)

---

## 概述

### 一句话概括
用户消息经 Guardian 前置审查 → Agent 流式执行（含 Tools 调用）→ SSE 分段响应 → 会话 JSON 持久化 → v2 模式后台蒸馏记忆。

### 涉及组件
- **入口**：`backend/api/chat.py:chat()`
- **Agent**：`backend/graph/agent.py:AgentManager.astream()`
- **工厂**：`backend/graph/agent_factory.py:create_agent_from_config()`
- **Guardian**：`backend/graph/guardian.py:GuardianMiddleware`
- **记忆**：`backend/memory_module_v2/integrations/middleware.py`
- **会话**：`backend/service/session_manager.py`

### 串行/并行说明
```
chat() 串行处理：
  1. 加载历史 → 2. 检索记忆 → 3. Agent 执行 → 4. 保存会话 → 5. 蒸馏

Agent 内部并行：
  - Guardian/Summarization middleware 串行前置
  - Tools 调用由 LLM 决定顺序（非并行）
```

---

## 请求入口：/api/chat

### 调用链路

```
HTTP POST /api/chat
  → chat.py:chat() [第47行]
      → session_manager.load_session_record()    [第52行]
      → build_request_context()                  [第57行]
      → agent_manager.astream()                 [第68行]
          → AgentManager._build_agent()          [agent.py:51]
          → AgentManager._build_messages()      [agent.py:59]
          → (v1) memory_indexer.retrieve()      [agent.py:95]
          → (v2 always) build_memory_context()  [agent.py:107]
          → agent.astream()                     [agent.py:133]
              → [LangChain Agent Graph 内部]
          → SSE yield token/tool_start/tool_end/done
      → session_manager.save_message()          [第97-104行]
      → (v2) _distill_session_background()      [第119-121行]
```

### 关键代码片段

**`backend/api/chat.py:46-58`**：请求入口
```python
@router.post("/chat")
async def chat(payload: ChatRequest):
    session_manager = agent_manager.session_manager
    if session_manager is None:
        raise HTTPException(status_code=503, detail="Agent manager is not initialized")

    # 加载该会话的历史记录
    history_record = session_manager.load_session_record(payload.session_id)
    # 判断是否首个用户消息（用于生成标题）
    is_first_user_message = not any(
        message.get("role") == "user"
        for message in history_record.get("messages", [])
    )
    # 构建请求上下文，包含 Langfuse callbacks
    request_context = build_request_context(thread_id=payload.session_id)
    # 对话历史由 checkpointer 管理，不再从 SessionManager 传入
    history_for_agent: list[dict[str, Any]] = []
```

---

## Agent 执行：流式响应处理

### 调用链路

```
AgentManager.astream() [agent.py:81]
  → AgentManager._build_agent() [agent.py:51]
      → build_agent_config() [agent_factory.py:62]
          → build_llm_config_from_settings()
          → get_checkpointer()
          → build_system_prompt()
      → create_agent_from_config() [agent_factory.py:90]
          → create_agent() from langchain.agents
          → 注入 GuardianMiddleware + SummarizationMiddleware

  → agent.astream() [LangChain 内部]

  → SSE 事件 yield:
      - token [agent.py:153-156]
      - tool_start [agent.py:175-189]
      - tool_end [agent.py:191-203]
      - done [agent.py:205-230]
```

### 关键代码片段

**`backend/graph/agent.py:81-137`**：流式处理核心
```python
async def astream(
    self,
    message: str,
    history: list[dict[str, Any]],
    context: RequestContext | None = None,
):
    if self.base_dir is None:
        raise RuntimeError("AgentManager is not initialized")

    memory_backend = get_memory_backend()
    turn_messages: list[dict[str, str]] = []

    # ========== 记忆注入逻辑 ==========
    if memory_backend == "v1":
        # v1: Chroma RAG 注入
        retrievals = memory_indexer.retrieve(message, top_k=3)
        yield {"type": "retrieval", "query": message, "results": retrievals}
        if retrievals:
            turn_messages.append({
                "role": "assistant",
                "content": self._format_retrieval_context(retrievals),
            })
    elif memory_backend == "v2" and get_memory_v2_inject_mode() == "always":
        # v2 强制注入：每轮自动检索
        try:
            v2_context = build_memory_context(message)
            if v2_context:
                yield {"type": "retrieval_v2", "query": message, "context": v2_context}
                turn_messages.append({"role": "assistant", "content": v2_context})
        except Exception as v2_exc:
            logger.warning("Memory v2 forced injection failed: %s", v2_exc)
    # v2 tool 模式：search_memory 作为工具注册，由 Agent 自主调用

    turn_messages.append({"role": "user", "content": message})

    # 构建 Agent
    agent = self._build_agent()
    run_config: dict[str, Any] = {
        "configurable": {"thread_id": (context.thread_id if context else "default")}
    }
    if context and context.callbacks:
        run_config["callbacks"] = context.callbacks

    # ========== 流式迭代 ==========
    async for mode, payload in agent.astream(
        {"messages": turn_messages},
        stream_mode=["messages", "updates"],
        config=run_config,
    ):
        if mode == "messages":
            chunk, metadata = payload
            # 优先从 metadata 中读取 usage
            usage_candidate = metadata.get("usage") if isinstance(metadata, dict) else None
            if isinstance(usage_candidate, dict):
                last_usage = usage_candidate

            # 只转发主 agent 节点的 token；跳过 guardian middleware 等的 LLM 输出
            node = metadata.get("langgraph_node") if isinstance(metadata, dict) else None
            if node is not None and node != "agent":
                continue

            text = _stringify_content(getattr(chunk, "content", ""))
            if text:
                final_content_parts.append(text)
                yield {"type": "token", "content": text}
            continue

        if mode != "updates":
            continue

        # 处理工具调用事件
        for update in payload.values():
            if not update:
                continue
            for agent_message in update.get("messages", []):
                message_type = getattr(agent_message, "type", "")
                tool_calls = getattr(agent_message, "tool_calls", []) or []

                # AI 消息无工具调用时记录
                if message_type == "ai" and not tool_calls:
                    candidate = _stringify_content(getattr(agent_message, "content", ""))
                    if candidate:
                        last_ai_message = candidate

                # 工具调用开始
                if tool_calls:
                    for tool_call in tool_calls:
                        call_id = str(tool_call.get("id") or tool_call.get("name"))
                        tool_name = str(tool_call.get("name", "tool"))
                        tool_args = tool_call.get("args", "")
                        if not isinstance(tool_args, str):
                            tool_args = json.dumps(tool_args, ensure_ascii=False)
                        pending_tools[call_id] = {
                            "tool": tool_name,
                            "input": str(tool_args),
                        }
                        yield {
                            "type": "tool_start",
                            "tool": tool_name,
                            "input": str(tool_args),
                        }

                # 工具调用结束
                if message_type == "tool":
                    tool_call_id = str(getattr(agent_message, "tool_call_id", ""))
                    pending = pending_tools.pop(
                        tool_call_id,
                        {"tool": getattr(agent_message, "name", "tool"), "input": ""},
                    )
                    output = _stringify_content(getattr(agent_message, "content", ""))
                    yield {
                        "type": "tool_end",
                        "tool": pending["tool"],
                        "output": output,
                    }
                    yield {"type": "new_response"}  # 工具完成后标记新响应段

    yield {"type": "done", "content": final_content}
```

---

## Guardian 安全审查

### 调用链路

```
Agent 执行前（LangChain Middleware 机制）
  → GuardianMiddleware.before_agent() [guardian.py:74-98]
      → last_user_text_from_agent_state()  [guardian.py:57-65]
      → evaluate_guardian_input()           [guardian.py:208]
          → _request_guardian_label()        [guardian.py:173-205]
              → ChatOpenAI.invoke()
          → parse_or_fallback_guardian_label()
      → 返回 {"jump_to": "end"} 或 None
```

### 关键代码片段

**`backend/graph/guardian.py:68-108`**：GuardianMiddleware
```python
class GuardianMiddleware(AgentMiddleware[AgentState[ResponseT], ContextT, ResponseT]):
    """LangChain AgentMiddleware：在 agent 图入口处（before_agent）做安全分类。"""

    @hook_config(can_jump_to=["end"])
    @override
    def before_agent(
        self,
        state: AgentState[ResponseT],
        runtime: Runtime[ContextT],
    ) -> dict[str, Any] | None:
        from config import get_settings

        if not get_settings().guardian_enabled:
            return None  # 未启用，直接放行

        # 从 agent state 中提取用户消息文本
        user_text = last_user_text_from_agent_state(state)
        # 调用 Guardian 评估
        result = evaluate_guardian_input(user_text)
        if result.is_blocked:
            logger.info(
                "Guardian blocked in before_agent: reason=%s label=%s",
                result.reason_code,
                result.label,
            )
            # 危险：跳到图末尾，返回拦截消息
            return {
                "jump_to": "end",
                "messages": [AIMessage(content=result.block_message)],
            }
        return None  # 安全：继续执行 Agent
```

**`backend/graph/guardian.py:208-237`**：评估函数
```python
def evaluate_guardian_input(user_text: str) -> GuardianRuntimeResult:
    from config import get_settings

    settings = get_settings()
    block_message = settings.guardian_block_message
    if not settings.guardian_enabled:
        return GuardianRuntimeResult(
            is_blocked=False, label="安全", reason_code="guardian_disabled", block_message=block_message,
        )

    try:
        raw = _request_guardian_label(user_text)  # 调用 LLM
        label = parse_or_fallback_guardian_label(raw, settings.guardian_fail_mode)
        reason = "guardian_dangerous" if label == "危险" else "guardian_ok"
    except Exception as error:
        label, reason = classify_guardian_error(
            status_code=None, fail_mode=settings.guardian_fail_mode, error=error,
        )

    return GuardianRuntimeResult(
        is_blocked=(label == "危险"),
        label=label,
        reason_code=reason,
        block_message=block_message,
    )
```

**`backend/graph/guardian.py:173-205`**：实际调用 LLM
```python
def _request_guardian_label(user_text: str) -> str:
    from config import get_settings
    settings = get_settings()

    from langchain_openai import ChatOpenAI

    # 构建 Guardian 专用客户端
    timeout_seconds = max(0.1, settings.guardian_timeout_ms / 1000.0)
    client = ChatOpenAI(
        model=settings.guardian_model,
        api_key=settings.guardian_api_key,
        base_url=settings.guardian_base_url,
        temperature=0,
        timeout=timeout_seconds,
    )

    # 构造请求：只有 system prompt + 用户消息
    payload = build_guardian_request_payload(
        user_text,
        model=settings.guardian_model,
        system_prompt=GUARDIAN_SYSTEM_PROMPT,
    )
    response = client.invoke(payload["messages"])
    content = getattr(response, "content", "")
    # 解析返回内容，只允许 "安全" 或 "危险"
    return content.strip()
```

---

## 记忆注入机制

### v2 混合检索流程

```
用户 Query
    │
    ├───→ dense_search() [dense.py]
    │        pg_vector.similarity_search()
    │        返回 Top-200 候选
    │
    ├───→ keyword_search() [keyword.py]
    │        BM25 评分排序
    │        返回 Top-200 候选
    │
    ▼
weighted_sum_fusion() / rrf_fusion() [fusion.py]
    │
    ▼
Top-K 最终结果
    │
    ▼
回跳原始证据（exchanges 表）
    │
    ▼
MemoryHit 对象 + verbatim_snippet
```

### 关键代码片段

**`backend/memory_module_v2/retrieval/service.py:25-82`**：检索入口
```python
def retrieval_search(
    query: str,
    *,
    mode: SearchMode = SearchMode.HYBRID_CROSS,
    top_k: int = 10,
    filters: MemorySearchFilters | None = None,
    debug: bool = False,
) -> MemorySearchResponse:
    config = get_memory_v2_config()
    session_ids = filters.session_ids if filters else None

    dense_candidates: list[dict[str, Any]] = []
    keyword_candidates: list[dict[str, Any]] = []

    # 1. pgvector 语义检索
    if mode in (SearchMode.DENSE_DISTILLED, SearchMode.HYBRID_CROSS):
        dense_candidates = dense_search(
            query,
            top_k=config.dense_top_k,
            session_ids=session_ids,
        )

    # 2. BM25 关键词检索
    if mode in (SearchMode.KEYWORD_VERBATIM, SearchMode.HYBRID_CROSS):
        keyword_candidates = keyword_search(
            query,
            top_k=config.keyword_top_k,
            session_ids=session_ids,
        )

    # 3. 融合
    if mode == SearchMode.DENSE_DISTILLED:
        fused = [{**c, "fused_score": c.get("dense_score", 0.0)} for c in dense_candidates[:top_k]]
    elif mode == SearchMode.KEYWORD_VERBATIM:
        fused = [{**c, "fused_score": c.get("keyword_score", 0.0)} for c in keyword_candidates[:top_k]]
    else:
        fusion_method = getattr(config, "fusion_method", "weighted_sum").lower()
        if fusion_method == "weighted_sum":
            fused = weighted_sum_fusion(
                dense_candidates, keyword_candidates,
                dense_weight=getattr(config, "dense_weight", 0.3),
                keyword_weight=getattr(config, "keyword_weight", 0.7),
                top_k=top_k,
            )
        else:
            fused = rrf_fusion(
                dense_candidates, keyword_candidates,
                k=getattr(config, "rrf_k", 60),
                top_k=top_k,
            )
    # ...
```

**`backend/memory_module_v2/integrations/middleware.py`**：v2 上下文注入
```python
def build_memory_context(message: str) -> str:
    # 根据 query 检索记忆，返回格式化字符串
    # 注入到 System Prompt 中
```

---

## 会话持久化

### 调用链路

```
chat() 处理完成
  → session_manager.save_message() [chat.py:97-104]
      → 读取 sessions/{session_id}.json
      → 追加 user/assistant 消息
      → 写入 JSON
```

### 关键代码片段

**`backend/api/chat.py:97-104`**：保存消息
```python
session_manager.save_message(payload.session_id, "user", payload.message)
for segment in segments:
    session_manager.save_message(
        payload.session_id,
        "assistant",
        segment["content"],
        tool_calls=segment["tool_calls"] or None,
    )
```

---

## 错误恢复流程

### Checkpointer 连接断开重试

**`backend/api/chat.py:123-139`**：
```python
except Exception as exc:
    if (
        not retried
        and not emitted_any_event
        and _is_recoverable_checkpointer_error(exc)  # 检查是否是连接断开错误
    ):
        retried = True
        logger.warning(
            "checkpointer connection dropped, reconnect and retry once: %s", exc
        )
        await reconnect_checkpointer_async()  # 重建连接
        continue  # 重试一次
    # 其他错误，打印日志并发送 error 事件
    print("[chat] error in event_generator", repr(exc))
    traceback.print_exc()
    yield _sse("error", {"error": str(exc)})
    return
```

**`backend/graph/checkpointer.py:78-91`**：重连逻辑
```python
async def reconnect_checkpointer_async() -> None:
    """重建 checkpointer 连接（用于数据库重启后连接失效场景）。"""
    global _default_saver, _postgres_cm
    async with _init_lock:
        old_cm = _postgres_cm
        _default_saver = None
        _postgres_cm = None
        if old_cm is not None:
            try:
                await old_cm.__aexit__(None, None, None)
            except Exception:
                pass  # 忽略关闭异常
        await _init_checkpointer_nolock()  # 重建连接
```

### 可恢复错误判断

**`backend/api/chat.py:37-43`**：
```python
def _is_recoverable_checkpointer_error(exc: Exception) -> bool:
    text = str(exc).lower()
    return (
        exc.__class__.__name__ == "OperationalError"
        or "could not receive data from server" in text
        or "software caused connection abort" in text
    )
```

---

## 关键代码片段汇总

### 1. AgentManager.astream() 核心流式处理

**文件**：`backend/graph/agent.py:81`
**行号**：81-137
**说明**：处理 SSE 事件的中心，包括 token、tool_start、tool_end、done 事件的分发

### 2. GuardianMiddleware.before_agent()

**文件**：`backend/graph/guardian.py:74`
**行号**：74-98
**说明**：LangChain Middleware 钩子，在 Agent 执行前拦截并做安全审查

### 3. evaluate_guardian_input()

**文件**：`backend/graph/guardian.py:208`
**行号**：208-237
**说明**：调用 LLM 判断用户消息是否安全，返回拦截或放行

### 4. retrieval_search() 检索入口

**文件**：`backend/memory_module_v2/retrieval/service.py:25`
**行号**：25-82
**说明**：协调 dense + keyword + fusion 的混合检索

### 5. distill_session_background()

**文件**：`backend/api/chat.py:151`
**行号**：151-175
**说明**：对话结束后的后台蒸馏任务，生成结构化记忆对象

### 6. AgentConfig 工厂

**文件**：`backend/graph/agent_factory.py:62`
**行号**：62-87
**说明**：根据配置构建 AgentConfig，包括 LLM、Tools、Prompt、Checkpointer

### 7. build_system_prompt()

**文件**：`backend/service/prompt_builder.py:62`
**行号**：62-83
**说明**：动态组装 System Prompt，从多个组件文件拼接

### 8. _is_recoverable_checkpointer_error()

**文件**：`backend/api/chat.py:37`
**行号**：37-43
**说明**：判断 Checkpointer 错误是否可恢复（连接断开类错误）
